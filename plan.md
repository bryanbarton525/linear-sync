# Plan

> Authored by the Architect. The initial section below is the primary plan; remediation cycles append `## Remediation Cycle N` sections and never rewrite this header.

## Overview

This workflow implements a mechanical file-writing and validation task for a Linear.app-to-PostgreSQL sync service. All 9 source files are pre-written and provided verbatim—no design decisions are required. The implementation follows a strict validation pipeline: write files → build → test → commit/push. The service uses Go 1.21 with PostgreSQL driver (lib/pq) and structured error handling. Core architecture: config loader, Linear GraphQL client, PostgreSQL storage layer with upsert, and a main loop with 5-minute ticker and graceful shutdown.

## Delivery Target

Compiled Go binary and committed source code in github.com/bryanbarton525/linear-sync main branch

## Tech Stack

- Go 1.21
- github.com/lib/pq v1.10.9 (PostgreSQL driver)
- github.com/stretchr/testify v1.9.0 (test assertions)
- PostgreSQL 12+ (runtime dependency)
- Linear.app GraphQL API

## Components

| Name | Description | Inputs | Outputs |
|---|---|---|---|
| Configuration Module | Loads LINEAR_API_KEY, LINEAR_TEAM_ID, and DATABASE_URL from environment variables with validation | Environment variables | Config struct with validated values |
| Linear API Client | GraphQL client for fetching issues from Linear.app. Handles request construction, response parsing, and error propagation | API key, Team ID, HTTP context | Slice of Issue structs with parsed fields |
| PostgreSQL Storage Layer | Manages database connection and upsert operations with transaction safety and prepared statements | Database connection string, Issue slice | Persisted issues in linear_issues table |
| Main Service Loop | Orchestrates periodic sync (5-minute ticker), signal handling for graceful shutdown, and operational logging | Config, Storage, Linear client | Running service with periodic sync and clean termination |
| Test Suite | Unit tests for config loading, Linear API mocking, and storage operations. Storage tests skip gracefully without PostgreSQL | Source files, Test fixtures | 4 PASS + 2 SKIP test results |

## Architectural Decisions

1. **Use verbatim file content without modification**
   - Rationale: All source files are pre-written and tested. Any deviation risks introducing compilation or test failures
   - Tradeoffs: No flexibility for improvements, but guarantees exact compliance with acceptance criteria
2. **Push to main branch (not workflow branch)**
   - Rationale: The request explicitly states 'push to main branch'. The workspace shows workflow/77e711cb-2688-4f21-9a5e-52b6f9bccb4e as current branch, but the user intent is clear: deliver to main
   - Tradeoffs: May require branch switch or force push depending on git state. Task description will handle both scenarios
3. **Storage tests must skip without PostgreSQL**
   - Rationale: Acceptance criteria explicitly states '2 SKIP' is expected and correct behavior when database is unavailable
   - Tradeoffs: Test coverage is reduced in CI environments without PostgreSQL, but this is intentional design
4. **Use -buildvcs=false flag for compilation**
   - Rationale: Disables VCS stamping to avoid build errors when git metadata is incomplete or unavailable
   - Tradeoffs: Binary loses version metadata, but ensures reliable build in any git state

## Task Graph

| ID | Specialty | Title | Depends On | Description |
|---|---|---|---|---|
| bc4704d2 | backend | Write All Source Files to Workspace | - | Produce 9 code artifacts in the workspace root directory (/var/lib/go-orca/workspaces/77e711cb-2688-4f21-9a5e-52b6f9bccb4e). Write each file VERBATIM with exact content provided—do not add, remove, or change any characters.  Artifacts to write: 1. `go.mod` (artifact kind: code) — Module definition with dependencies github.com/lib/pq v1.10.9 and github.com/stretchr/testify v1.9.0 2. `go.sum` (artifact kind: code) — Checksums for all dependencies 3. `main.go` (artifact kind: code) — Entry point with signal handling, ticker loop, and sync orchestration 4. `config.go` (artifact kind: code) — Configuration loader reading LINEAR_API_KEY, LINEAR_TEAM_ID, DATABASE_URL from environment 5. `linear.go` (artifact kind: code) — Linear.app GraphQL client with fetchIssues method 6. `storage.go` (artifact kind: code) — PostgreSQL storage layer with newDB and upsert functions 7. `config_test.go` (artifact kind: code) — Unit tests for config loading with 4 test cases 8. `linear_test.go` (artifact kind: code) — Unit tests for Linear client with httptest mock server 9. `storage_test.go` (artifact kind: code) — Integration tests for storage upsert with PostgreSQL (skip if unavailable)  Acceptance criteria: - All 9 files written to workspace root with exact content from provided specification - No additional files created - File content is byte-for-byte identical to provided text - Do NOT run go mod init, go mod tidy, or go work init - Use write_file tool for each file (can be parallelized) |
| edf451d7 | backend | Validate Go Build | bc4704d2 | Produce validation artifact kind `build_result` by compiling the Go source code. Execute `go build -buildvcs=false .` from the workspace directory (/var/lib/go-orca/workspaces/77e711cb-2688-4f21-9a5e-52b6f9bccb4e).  Acceptance criteria: - Command exits with status 0 (success) - No compilation errors in stderr - Binary file `linear-sync` is created in workspace root - Build uses only dependencies from go.mod (lib/pq, testify) - The -buildvcs=false flag must be present to disable VCS stamping  If build fails, report exact error message including file, line, and error description. Do not proceed to next task on failure. |
| 4f3eb5df | backend | Validate Test Suite | edf451d7 | Produce validation artifact kind `test_result` by executing the test suite. Run `go test -buildvcs=false . -v` from the workspace directory (/var/lib/go-orca/workspaces/77e711cb-2688-4f21-9a5e-52b6f9bccb4e).  Acceptance criteria: - Command completes successfully (exit code 0) - Test output shows exactly 4 PASS results:   * TestLoad (config_test.go) — validates all 4 test cases   * TestFetchIssues (linear_test.go) — validates successful API response parsing   * TestFetchIssues_APIError (linear_test.go) — validates error handling   * TestStorage_UpsertEmpty (storage_test.go) — validates empty slice handling (may skip if DB unavailable) - Test output shows exactly 2 SKIP results:   * TestStorage_Upsert (storage_test.go) — skips if PostgreSQL unavailable   * TestStorage_UpsertEmpty (storage_test.go) — skips if PostgreSQL unavailable - Total: 4 PASS + 2 SKIP (or 6 PASS + 0 SKIP if PostgreSQL is available) - No test failures (FAIL) allowed - The -buildvcs=false flag must be present  If test results do not match expected pattern, report exact test names and outcomes. Do not proceed to next task on failure. |
| 9c76e14e | backend | Commit and Push to GitHub | 4f3eb5df | Produce artifact kind `git_commit` by committing all source files and pushing to github.com/bryanbarton525/linear-sync main branch. Execute from workspace directory (/var/lib/go-orca/workspaces/77e711cb-2688-4f21-9a5e-52b6f9bccb4e).  Steps: 1. Check current branch with `git branch --show-current` 2. If not on main branch, switch: `git checkout main` or create: `git checkout -b main` 3. Stage all files: `git add .` 4. Commit with message: `git commit -m "Implement Linear.app sync service  Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>"` 5. Push to remote: `git push origin main` (may need `-u` flag if branch is new)  Acceptance criteria: - Current branch is main after checkout step - All 9 source files are staged and committed - Commit message includes Co-authored-by trailer - Push completes successfully to github.com/bryanbarton525/linear-sync - Remote main branch contains all committed files  If git authentication fails, report the error and suggest credential configuration (SSH key or personal access token). If push is rejected (non-fast-forward), report conflict details. |

---

## Remediation Cycle 1 — PM Triage

Root cause: Implementation defect in task bc4704d2. The Pod failed to write any of the 9 required source files to the workspace despite having write_file tool available. The implementation-summary artifact incorrectly reported success, but workspace_preflight validation confirms zero Go source files exist in /var/lib/go-orca/workspaces/77e711cb-2688-4f21-9a5e-52b6f9bccb4e. This cascaded into failed build validation, failed test validation, and a premature git commit with no source code. No requirement gap, design gap, or environment failure—purely an execution failure. Remediation: Pod must retry task bc4704d2 using write_file tool to create all 9 files verbatim from task specification. No Constitution amendment required—all requirements are clear and complete. Build and test validation will auto-proceed once source files are written. Retry commit task only after validation passes.

**QA blocking issues being triaged:**

- [Source Files] Workspace validation found no Go source files in /var/lib/go-orca/workspaces/77e711cb-2688-4f21-9a5e-52b6f9bccb4e. Pod must write the source files before Go validation can run.
- [Source Files] Workspace validation confirms no Go source files exist in /var/lib/go-orca/workspaces/77e711cb-2688-4f21-9a5e-52b6f9bccb4e. The implementation-summary artifact claims files were written, but workspace_preflight validation proves they are not present. All 9 required files (go.mod, go.sum, main.go, config.go, linear.go, storage.go, config_test.go, linear_test.go, storage_test.go) are missing.: Use the write_file tool to create all 9 source files in the workspace directory with exact verbatim content from the task specification. Verify each write_file call completes successfully before proceeding to build validation.
- [Build Validation] Task edf451d7 (Validate Go Build) cannot execute because prerequisite task bc4704d2 failed to produce source files. Acceptance criterion 'go build -buildvcs=false . completes successfully with no compilation errors' cannot be validated without source code present.: After writing all source files, execute 'go build -buildvcs=false .' from workspace directory and verify exit code 0 with binary artifact 'linear-sync' created.
- [Test Validation] Task 4f3eb5df (Validate Test Suite) cannot execute because prerequisite build validation has not completed. Acceptance criterion 'go test -buildvcs=false . -v shows exactly 4 PASS and 2 SKIP results' cannot be validated without compiled code.: After successful build, execute 'go test -buildvcs=false . -v' and verify output matches expected pattern: 4 PASS (TestLoad, TestFetchIssues, TestFetchIssues_APIError, TestStorage_UpsertEmpty) and 2 SKIP (storage tests without PostgreSQL).
- [Git Commit] Task 9c76e14e (Commit and Push to GitHub) was executed prematurely before validation tasks completed. The git-push-result artifact states 'Attempted to commit and push' but without verified source files, build success, and test validation, the commit cannot contain valid deliverables. Constitution acceptance criteria require all source files written and validated before git push.: Execute git commit and push only after all validation steps pass. Verify workspace contains all 9 files, build completes successfully, and tests show 4 PASS + 2 SKIP before staging, committing, and pushing to main branch.

---

## Remediation Cycle 1 — Architect

**Current overview:** This workflow implements a mechanical file-writing and validation task for a Linear.app-to-PostgreSQL sync service. All 9 source files are pre-written and provided verbatim—no design decisions are required. The implementation follows a strict validation pipeline: write files → build → test → commit/push. The service uses Go 1.21 with PostgreSQL driver (lib/pq) and structured error handling. Core architecture: config loader, Linear GraphQL client, PostgreSQL storage layer with upsert, and a main loop with 5-minute ticker and graceful shutdown.

### Remediation Tasks

| ID | Specialty | Title | Depends On | Description |
|---|---|---|---|---|
| b60a6aed | backend | Retry Writing All Source Files with Verification | - | Produce 9 code artifacts in workspace /var/lib/go-orca/workspaces/77e711cb-2688-4f21-9a5e-52b6f9bccb4e using write_file tool. Write each file VERBATIM with exact content from specification—do not add, remove, or change any characters.  Verification sequence: 1. Pre-write check: Verify workspace is writable by executing 'touch /var/lib/go-orca/workspaces/77e711cb-2688-4f21-9a5e-52b6f9bccb4e/test.tmp && rm /var/lib/go-orca/workspaces/77e711cb-2688-4f21-9a5e-52b6f9bccb4e/test.tmp'. If this fails, report workspace permission error immediately. 2. Write files: Call write_file tool for all 9 files in parallel (single response with 9 tool calls):    - go.mod (module definition with lib/pq and testify dependencies)    - go.sum (dependency checksums)    - main.go (service entry point with signal handling and ticker loop)    - config.go (environment variable loader for LINEAR_API_KEY, LINEAR_TEAM_ID, DATABASE_URL)    - linear.go (GraphQL client with fetchIssues method)    - storage.go (PostgreSQL storage layer with newDB and upsert)    - config_test.go (unit tests for config loading with 4 test cases)    - linear_test.go (unit tests with httptest mock server)    - storage_test.go (integration tests that skip without PostgreSQL) 3. Post-write verification: Execute 'ls -la /var/lib/go-orca/workspaces/77e711cb-2688-4f21-9a5e-52b6f9bccb4e/*.go /var/lib/go-orca/workspaces/77e711cb-2688-4f21-9a5e-52b6f9bccb4e/go.mod /var/lib/go-orca/workspaces/77e711cb-2688-4f21-9a5e-52b6f9bccb4e/go.sum' to confirm all 9 files exist and are readable.  Acceptance criteria: - Workspace writability confirmed before file writes - All 9 files written with exact verbatim content (byte-for-byte identical to specification) - Post-write verification confirms all 9 files exist - No additional files created - Do NOT run go mod init, go mod tidy, or go work init  After this task completes successfully, automated build validation will trigger automatically (go build -buildvcs=false .) followed by test validation (go test -buildvcs=false . -v). |
| bd1c1b31 | backend | Commit and Push Validated Source Code to Main Branch | - | Produce git_commit artifact by staging, committing, and pushing all validated source files to github.com/bryanbarton525/linear-sync main branch. Execute from workspace /var/lib/go-orca/workspaces/77e711cb-2688-4f21-9a5e-52b6f9bccb4e.  CRITICAL: Execute this task ONLY after automated validation completes successfully: - go build -buildvcs=false . must complete with exit code 0 and produce binary 'linear-sync' - go test -buildvcs=false . -v must show 4 PASS + 2 SKIP (or 6 PASS + 0 SKIP if PostgreSQL available) - All 9 source files must exist in workspace  If validation has not completed or failed, DO NOT proceed with git operations. Report validation status and wait.  Git operation sequence: 1. Verify prerequisites: Check that binary 'linear-sync' exists and validation passed 2. Check current branch: git branch --show-current 3. Switch to main branch: git checkout main (or git checkout -b main if branch does not exist) 4. Stage all files: git add . 5. Commit with proper message:    git commit -m "Implement Linear.app sync service        Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>" 6. Push to remote: git push origin main (add -u flag if creating new upstream branch)  Acceptance criteria: - Validation completed successfully before git operations start - Current branch is main after checkout step - All 9 source files (go.mod, go.sum, main.go, config.go, linear.go, storage.go, config_test.go, linear_test.go, storage_test.go) are staged and committed - Commit message includes Co-authored-by trailer exactly as specified - Push completes successfully to github.com/bryanbarton525/linear-sync - Remote main branch contains all committed files  Error handling: - If git authentication fails, report error message and suggest SSH key or personal access token configuration - If push is rejected (non-fast-forward), report conflict details and suggest resolution strategy - If validation has not completed, report current validation status and do not proceed |

---

## Remediation Cycle 2 — PM Triage

All five QA blocking issues stem from a single implementation defect: task b60a6aed failed to write any of the 9 required source files to the workspace directory. The write_file tool calls were not executed successfully, leaving the workspace empty and cascading into failed build, test, and git validation. Classification: all issues are implementation defects or validation failures dependent on the source file write failure. No requirement gap—the Constitution is complete and unambiguous. No design gap—the architecture is frozen. Remediation: Task b60a6aed must be retried using explicit write_file tool calls with pre- and post-write verification checks. Once files are written successfully, automated build and test validation will proceed automatically. After validation passes with expected results (4 PASS + 2 SKIP), task bd1c1b31 will execute git commit and push to main branch. The remediation task graph already includes proper prerequisite gating to prevent premature git operations.

**QA blocking issues being triaged:**

- [Source Files] Workspace validation found no Go source files in /var/lib/go-orca/workspaces/77e711cb-2688-4f21-9a5e-52b6f9bccb4e. Pod must write the source files before Go validation can run.
- [Source Files] Workspace validation confirms no Go source files exist in /var/lib/go-orca/workspaces/77e711cb-2688-4f21-9a5e-52b6f9bccb4e. The two artifacts provided (implementation-summary and git-push-result) are status messages, not actual source code files. All 9 required files (go.mod, go.sum, main.go, config.go, linear.go, storage.go, config_test.go, linear_test.go, storage_test.go) remain missing after remediation cycle 1.: Task b60a6aed must successfully execute write_file tool calls for all 9 files. Verify write_file tool is available and functional. Confirm workspace directory permissions allow file creation. After writing, verify file existence with ls command before declaring task complete.
- [Build Validation] Constitution acceptance criterion 'go build -buildvcs=false . completes successfully with no compilation errors' cannot be validated. The go toolchain validation result shows workspace_preflight failure due to missing source files.: Build validation will auto-proceed once all 9 source files are successfully written to workspace. No separate remediation action needed for this component.
- [Test Validation] Constitution acceptance criterion 'go test -buildvcs=false . -v shows exactly 4 PASS and 2 SKIP results' cannot be validated without compiled code. Test suite execution depends on successful build.: Test validation will auto-proceed after build succeeds. No separate remediation action needed for this component.
- [Git Deliverable] Task bd1c1b31 (Commit and Push to GitHub) cannot proceed without validated source files. The git-push-result artifact indicates an attempt was made, but pushing empty or incomplete content violates Constitution requirement that 'code is committed and pushed to github.com/bryanbarton525/linear-sync on main branch' with all deliverables present.: Execute git commit and push ONLY after workspace contains all 9 files AND validation passes with expected results (4 PASS + 2 SKIP). Task bd1c1b31 description already includes correct prerequisite checks.

---

## Remediation Cycle 2 — Architect

**Current overview:** This workflow implements a mechanical file-writing and validation task for a Linear.app-to-PostgreSQL sync service. All 9 source files are pre-written and provided verbatim—no design decisions are required. The implementation follows a strict validation pipeline: write files → build → test → commit/push. The service uses Go 1.21 with PostgreSQL driver (lib/pq) and structured error handling. Core architecture: config loader, Linear GraphQL client, PostgreSQL storage layer with upsert, and a main loop with 5-minute ticker and graceful shutdown.

### Remediation Tasks

| ID | Specialty | Title | Depends On | Description |
|---|---|---|---|---|
| 434a05af | backend | Filesystem Persistence and Tool Availability Diagnostic | - | Produce diagnostic_result artifact by verifying workspace filesystem persistence and write_file tool availability before attempting source file writes.  Diagnostic steps: 1. Tool availability check: Confirm write_file tool is in agent's available tools list. If not present, report 'write_file tool not available' and STOP — cannot proceed without file writing capability. 2. Filesystem persistence test: Create test file /var/lib/go-orca/workspaces/77e711cb-2688-4f21-9a5e-52b6f9bccb4e/persistence_test.tmp with content 'test' using write_file tool. Then use view tool to read it back. Then delete it with bash rm command. If any step fails, report 'filesystem persistence failure' and STOP. 3. Workspace permissions check: Execute 'ls -la /var/lib/go-orca/workspaces/77e711cb-2688-4f21-9a5e-52b6f9bccb4e' to confirm directory exists and is writable.  Acceptance criteria: - write_file tool confirmed available - Test file created, read back successfully, and deleted - Workspace directory exists with write permissions - If any check fails, task reports failure with specific diagnostic (which step failed)  This diagnostic isolates whether the environment supports persistent file writes before attempting to write 9 source files for the third time. |
| f6501bdb | backend | Write Source Files Sequentially with Per-File Verification | - | Produce 9 code artifacts in workspace /var/lib/go-orca/workspaces/77e711cb-2688-4f21-9a5e-52b6f9bccb4e using sequential write-verify cycles. Write each file VERBATIM with exact content from specification—do not add, remove, or change any characters.  Sequential write-verify procedure (repeat for each of 9 files): 1. Write file using write_file tool with exact content 2. Immediately verify with: ls -la /var/lib/go-orca/workspaces/77e711cb-2688-4f21-9a5e-52b6f9bccb4e/<filename> 3. If file does not exist, STOP and report which file failed 4. Proceed to next file only after verification passes  Files to write (in this order): 1. go.mod — Module definition: module linear-sync; go 1.21; require github.com/lib/pq v1.10.9 and github.com/stretchr/testify v1.9.0; indirect requires davecgh/go-spew, pmezard/go-difflib, gopkg.in/yaml.v3 2. go.sum — Checksums for all dependencies (8 lines with hashes) 3. config.go — Config struct and load() function reading LINEAR_API_KEY, LINEAR_TEAM_ID, DATABASE_URL from environment 4. linear.go — linearClient struct with fetchIssues method, Issue/Assignee types, GraphQL query construction, JSON response parsing 5. storage.go — Storage struct with newDB and upsert methods, PostgreSQL connection, transaction handling, prepared statement upsert 6. main.go — Entry point with signal.NotifyContext, time.Ticker loop, doSync closure, graceful shutdown 7. config_test.go — TestLoad function with 4 test cases (missing API key, missing team ID, missing DB URL, valid config) 8. linear_test.go — TestFetchIssues and TestFetchIssues_APIError with httptest.NewServer mocks 9. storage_test.go — TestStorage_Upsert and TestStorage_UpsertEmpty (both skip if PostgreSQL unavailable)  Acceptance criteria: - Each file written sequentially with immediate verification - All 9 files confirmed present after write cycle completes - File content is byte-for-byte identical to provided specification - Post-verification: Execute 'ls -la /var/lib/go-orca/workspaces/77e711cb-2688-4f21-9a5e-52b6f9bccb4e/*.go /var/lib/go-orca/workspaces/77e711cb-2688-4f21-9a5e-52b6f9bccb4e/go.*' shows all 9 files - Do NOT run go mod init, go mod tidy, or go work init  After this task completes successfully, automated build validation will trigger (go build -buildvcs=false .) followed by test validation (go test -buildvcs=false . -v).  If this task fails with same symptom as previous two cycles (reports success but files don't persist), recommend escalation to 'QA exhausted' per two-cycle convergence rule. |
| 4a9dda96 | backend | Commit and Push Validated Source Code to Main Branch | - | Produce git_commit artifact by staging, committing, and pushing all validated source files to github.com/bryanbarton525/linear-sync main branch. Execute from workspace /var/lib/go-orca/workspaces/77e711cb-2688-4f21-9a5e-52b6f9bccb4e.  CRITICAL: Execute this task ONLY after automated validation completes successfully: - go build -buildvcs=false . must complete with exit code 0 and produce binary 'linear-sync' - go test -buildvcs=false . -v must show 4 PASS + 2 SKIP (or 6 PASS + 0 SKIP if PostgreSQL available) - All 9 source files must exist in workspace  If validation has not completed or failed, DO NOT proceed with git operations. Report validation status and wait.  Git operation sequence: 1. Verify prerequisites: Check that binary 'linear-sync' exists and validation passed 2. Check current branch: git branch --show-current 3. Switch to main branch: git checkout main (or git checkout -b main if branch does not exist) 4. Stage all files: git add . 5. Commit with proper message:    git commit -m "Implement Linear.app sync service        Co-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>" 6. Push to remote: git push origin main (add -u flag if creating new upstream branch)  Acceptance criteria: - Validation completed successfully before git operations start - Current branch is main after checkout step - All 9 source files (go.mod, go.sum, main.go, config.go, linear.go, storage.go, config_test.go, linear_test.go, storage_test.go) are staged and committed - Commit message includes Co-authored-by trailer exactly as specified - Push completes successfully to github.com/bryanbarton525/linear-sync - Remote main branch contains all committed files  Error handling: - If git authentication fails, report error message and suggest SSH key or personal access token configuration - If push is rejected (non-fast-forward), report conflict details and suggest resolution strategy - If validation has not completed, report current validation status and do not proceed |

